# 课程表 24 小时与 PC/手机端部署

## 已修复

1. 课程表周时间轴从 07:00-22:00 扩展为 00:00-24:00。
2. 00:00 标签从时间轴顶部开始显示，避免首个时间标签被裁剪。
3. 课程块按 24 小时坐标定位，早间和深夜课程都能显示。
4. 手机端使用同一时间轴结构，支持横向滑动查看七天课程列。
5. 手机端缩小时间标签与课程块字号，减少七列布局挤压。
6. 同步桌面端、`www` 发布端和手机端样式资源。

## 部署

- `www` 静态网页已重新部署。
- PC 根目录静态网页已单独部署。
- 两个分享链接均已验证可访问。

## 当前边界

- 手机端需要横向滑动查看完整七天；这是为了保留时间轴的可读性。
- 课程拖拽调整、冲突并排布局、颜色自定义仍未加入。