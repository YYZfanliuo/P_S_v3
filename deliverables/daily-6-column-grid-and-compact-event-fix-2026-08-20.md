# 日程与课程日视图：24×6 网格与短色块修复

日期：2026-08-20

## 本次改动

- 日程日视图改为 24 小时 × 6 个十分钟刻度的网格图。
- 课程表日视图复用同一套日网格，不再使用此前的单列 Timeline 色块布局。
- 左侧固定 24 个整点标签，右侧每小时包含 00、10、20、30、40、50 六个分钟刻度。
- 事件按开始/结束分钟精确定位到对应小时和十分钟网格内，跨小时事件会分段显示。
- 短色块采用自适应信息层级：正常尺寸显示类型、标题和时间；较窄色块隐藏次要信息；极短色块显示标题首字，并保留完整 `title`、无障碍标签和悬浮提示。
- 手机端使用独立横向滚动的日网格，保留完整 24 小时和 6 列刻度，不再依赖页面整体撑宽。
- 点击课程或日程色块仍可进入原编辑器。

## 验证

- `scripts/app.js` 语法检查通过。
- `www/scripts/app.js` 语法检查通过。
- `git diff --check` 通过。
- 本地预览 `http://127.0.0.1:4174/` 返回 `200 OK`。

## 说明

本次重点解决日视图网格和短色块信息溢出问题；周视图仍沿用统一 Timeline 周表结构，后续可继续增加冲突事件并排布局、当前时间线和显式缩放。