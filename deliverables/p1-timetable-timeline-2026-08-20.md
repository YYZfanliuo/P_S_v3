# P1：课程表时间轴周视图

## 已完成

- 课程表周视图从七列课程卡片改为“时间纵轴 + 七天横轴”的时间表。
- 时间范围为 07:00-22:00，课程按照开始和结束时间在对应日期列中定位。
- 课程块展示课程名称、时间和地点。
- 保留学期当前周、起止周、单双周和自定义周次过滤。
- 点击课程块仍可打开课程编辑器。
- 桌面端与 `www` 发布端脚本和样式已同步。

## 验证

- `scripts/app.js` Node 语法检查通过。
- `www/scripts/app.js` Node 语法检查通过。
- `git diff --check` 通过。

## 当前边界

- 时间轴暂时固定显示 07:00-22:00；早于 07:00 或晚于 22:00 的课程会被裁剪显示。
- 当前先解决周视图的可读性，尚未加入拖拽调整、课程冲突并排布局和颜色自定义。