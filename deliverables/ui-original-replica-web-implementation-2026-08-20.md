# 原版 UI 复刻回迁实施记录

日期：2026-08-20

## 本轮完成

已将 Ardot 画布中的原版复刻方向回迁到真实网页，目标是方便在原有网页基础上继续微调。

## 修改范围

- `styles/style.css`
  - 保持原版暖灰页面底色、浅色卡片、墨色主色和低饱和模块色。
  - 强化首页分组标题、Bento 卡片、Hero/时钟卡、快速记录卡、课程/概览卡的圆角、间距、阴影和层级。
  - 保留既有数据渲染和交互逻辑。
- `styles/mobile.css`
  - 同步移动端顶部栏、菜单按钮、时钟卡、单列卡片和概览/番茄钟的原版视觉层级。
  - 保持移动端已有响应式与时间视图逻辑。

## 未修改

- `scripts/app.js` 未修改，现有首页、课程、日程、计划和记录逻辑保持不变。
- 未改动业务数据模型、事件绑定和本地存储。

## 备份

原始文件已备份至：

`backups/ui-replica-2026-08-20-1148/`

包含：

- `index.html`
- `mobile.html`
- `styles/style.css`
- `styles/mobile.css`
- `scripts/app.js`

## 验证

- Node.js 脚本语法检查通过。
- `git diff --check` 通过。
- 桌面端本地预览：`http://127.0.0.1:4175/` 返回 200。
- 移动端本地预览：`http://127.0.0.1:4175/mobile.html` 返回 200。

## 下一步建议

建议先在浏览器中确认首页的整体密度，再决定是否继续调整：

1. Hero 高度与问候文字大小；
2. 今日聚焦、快速记录、今日日程三张卡的比例；
3. 今日课程卡是否继续保持整行；
4. 移动端首屏是否需要减少一张卡片；
5. 确认后再同步 `www/` 发布目录，避免过早覆盖线上静态资源。
