# 个人工作台项目交接说明

> 交接日期：2026-08-19
> 当前版本：v1.6.0
> GitHub 仓库：https://github.com/YYZfanliuo/P_S_v3

## 项目定位

个人工作台是一款面向个人日常管理的移动端 App 与桌面端 Web 工具，集中处理每日计划、习惯打卡、阅读与学习进度、运动、记账、日记、热点收藏、食谱、项目管理、课程表和日程。

当前产品重点是“首页快速了解今天，功能页面负责持续记录”。移动端通过抽屉侧边栏导航，桌面端使用固定侧边栏导航。

## 当前状态

- 移动端 Android App 已可构建 Debug APK。
- 移动端 Web 地址：`https://yyzfanliuo.github.io/P_S_v3/www/mobile.html`
- 桌面端 Web 地址：`https://yyzfanliuo.github.io/P_S_v3/`
- GitHub Pages 已部署。
- 页面切换不显示同步提示。
- 手动 Git 连接、手动上传、手动拉取仍显示同步提示。
- 课程表功能页支持日视图和周视图。
- 首页课程表使用大卡片展示。
- 头像资源已放入 `www/assets/avatar.jpg`。

## 导航分组

侧边栏必须保持以下顺序和分组：

### 今日行动

- 待办
- 习惯打卡
- 日程管理

### 打卡追踪

- 阅读打卡
- 每日锻炼
- 记账本
- 学习进度

### 内容记录

- 心情日记
- 今日热点
- 食谱与餐单
- 项目管理
- 我的课程表

### 统计

- 洞察复盘

### 其他

- 深色模式
- 设置

## 首页结构

首页按以下顺序组织：

1. 今日行动：问候、今日聚焦、待办、日程和快速记录。
2. 今日课程：课程表独占大卡片，显示当天课程概览。
3. 打卡追踪：概览圆环、习惯、阅读、运动、学习等进度。
4. 内容记录：支出、趋势、专注及其他长期积累信息。

## 关键文件

| 路径 | 作用 |
|---|---|
| `scripts/app.js` | 桌面端核心逻辑、模块配置、首页和导航渲染 |
| `www/scripts/app.js` | GitHub Pages 与移动端使用的前端逻辑 |
| `styles/style.css` | 桌面端样式 |
| `styles/mobile.css` | 移动端样式 |
| `www/index.html` | GitHub Pages 桌面端入口 |
| `www/mobile.html` | GitHub Pages 移动端入口 |
| `www/assets/avatar.jpg` | 移动端头像资源 |
| `android/` | Capacitor Android 工程 |
| `capacitor.config.json` | Capacitor 配置 |
| `package.json` | npm 脚本与依赖 |
| `手机App使用文档.html` | 面向用户的手机 App 使用说明 |

## 本地运行

在项目根目录执行：

```powershell
npm install
npm run dev
```

常用地址：

- 桌面端：`http://localhost:5173/`
- 移动端：`http://localhost:5173/www/mobile.html`

## Android 构建

需要 Android Studio 和可用的 JDK。构建前确认 `JAVA_HOME` 指向 Android Studio 自带的 JBR，然后执行：

```powershell
npm run build:android
```

Debug APK 通常位于：

```text
android/app/build/outputs/apk/debug/app-debug.apk
```

构建产物不建议提交到 Git 仓库。发布 APK 时可以通过新的 Release 流程或其他文件分发服务单独提供。

## GitHub Pages 部署

GitHub Pages 使用 `www/` 目录作为发布内容。修改桌面端或移动端页面后，需要确认对应改动已经同步到 `www/`，再提交并推送：

```powershell
git status
git add www scripts styles
git commit -m "describe the change"
git push origin main
```

推送后，等待 GitHub Actions 完成，再访问桌面端和移动端地址进行检查。

## 交接检查重点

新平台接手时，优先确认以下内容：

1. 桌面端和移动端是否使用同一份数据结构。
2. `scripts/app.js` 与 `www/scripts/app.js` 是否保持功能同步。
3. 侧边栏是否按上述分组显示，而不是把模块平铺。
4. 首页课程表是否仍为大卡片。
5. 课程表日视图和周视图能否切换。
6. 页面切换是否保持静默。
7. 手动上传和拉取是否仍显示状态提示。
8. `www/assets/avatar.jpg` 是否能在 GitHub Pages 正常加载。
9. `@vite/client` 不应作为生产部署依赖；如果生产页面仍请求它，应检查入口文件和构建发布配置。

## 已知问题

- GitHub Pages 历史部署曾出现 `@vite/client` 请求失败。该请求不影响静态页面主要显示，但生产版本应移除开发环境注入的 Vite 客户端引用。
- GitHub Pages 可能存在缓存。更新静态资源时建议增加版本参数或等待部署缓存刷新。
- 数据默认保存在当前设备和当前浏览器中。换设备、换浏览器或清除浏览记录可能导致本地数据无法继续使用；跨设备使用需要配置并手动执行同步。

## 后续建议

- 清理生产入口中的开发环境脚本引用。
- 把桌面端和移动端共享配置抽成单独模块，减少双份代码漂移。
- 为 Release APK 建立独立版本号和签名发布流程。
- 为课程表周视图补充真实数据测试。
- 在新平台接手后先保留当前 GitHub 仓库，确认新平台构建和部署稳定后再迁移远端。

