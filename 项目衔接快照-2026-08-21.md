# 个人工作台项目衔接快照

> 更新时间：2026-08-21 15:49（中国标准时间）
> 用途：新对话接续开发，减少重复探索和上下文消耗
> 当前分支：main
> 当前 HEAD：e597bb5 add project handoff documentation
> 当前版本标记：package.json v1.6.0（AI/开发日志改动尚未形成新版本号）

## 一、项目定位与协作约定

- 项目是个人工作台：桌面端 Web + 移动端 Web + Capacitor Android App。
- 当前重点：先验证 PC 端 AI 判断与建议价值，商业化暂缓。
- 当前处于开发者使用和调试阶段：运行日志优先完整记录现场，暂不提前做脱敏、加密、生产化和多版本兼容。
- 用户不懂编程。功能问题先用非技术语言说明现象、影响、原因、方案和取舍，用户确认后再修改；无须确认的检查、同步、汇报可以直接做。
- UI 问题先提供中文填空模板，帮助用户描述页面位置、设备、现象、期望、参考样式和限制。
- 修改前备份；暂不提交 Git、推送或部署，除非用户明确改变要求。
- AI 原则：AI 不仅负责秘书式安排，还负责计划中心的目标拆解、计划塑形和任务生成；简易、专业、智能三种模式都可以调用 AI 生成任务。系统负责事实、校验、保存和执行；用户拥有最终决定权。AI 不得未经确认删除任务、改截止日期或大范围重排时间块。

## 二、已完成的主要能力

### 基础工作台

- 待办、习惯打卡、阅读、运动、记账、日记、热点、学习、食谱、项目、课程表、日程。
- 数据主要保存在浏览器 localStorage，可通过 GitHub Gist 手动同步。
- 桌面端固定侧栏；移动端独立页面和底部导航/层级交互已做过第一阶段调整。
- 计划链路已建立：Goal → Milestone → Deliverable → Task → ScheduleBlock。
- 计划中心已有多目标、归档、任务编辑、预计时长、优先级、时间块锁定/重排、待办关联、今日执行、实际结果、延期/阻塞、局部重排、复盘统计；计划中心包含简易、专业、智能三种模式，三种模式均应支持调用 AI 生成任务，最终统一进入任务/计划预览与写入流程。
- 日程/课程表已有日视图、周视图、跨小时事件分段和时间块展示。

### AI

- AI 分为两类连续能力：计划中心中的“目标/计划/任务生成”，以及后台/今日执行中的“持续安排、执行跟踪和动态调整”。两者共用工作台数据、权限策略和开发日志。
- 简易、专业、智能三种计划模式都可以调用 AI 生成任务；模式差异是输入方式、信息充分度、追问方式和默认写入权限，不是 AI 能力开关。
- PC 设置页提供极简 AI 配置：接口地址、API Key、模型名称。
- AI 请求通过本机中转层 `local-ai-bridge.js`，监听 `127.0.0.1:5174`：
  - `GET /api/ai/status`
  - `POST /api/ai/chat`
- 中转层使用 Bearer API Key，按标准聊天接口格式转发。
- 今日执行页提供“AI 今日判断”：分析优先级、风险、建议和下一步；当前采纳/忽略只记录决定。已新增计划中心真实 AI 任务生成：三种模式统一调用结构化计划接口，解析任务、里程碑、交付物、假设和信息缺口，失败时使用本地规则草案；计划写入、任务状态变化、今日判断、秘书开始执行和局部重排均记录 AI 动作。计划中心新增“AI 后台动作”运行状态卡和动作可读标签，便于直接查看最近一次后台行为及其结果；智能模式可填写 AI 信息缺口并根据补充信息重新生成计划；计划生成增加请求进行中保护和请求序号校验，避免重复提交与旧结果覆盖；计划中心新增“让 AI 秘书巡检一次”，可让真实 AI 选择当前最适合推进的任务，或在失败时使用本地判断，并在授权开启时开始任务/局部重排。后续可继续扩展后台持续运行和更细的动作回放。
- 已修复上一轮发现的输出问题：
  - 默认 `max_tokens` 由 1200 提高到 3600；
  - 单次分析可传 `maxTokens: 3600`，二次整理传 1800；
  - 不再把 `reasoning_content` 当正式回答；
  - `finish_reason=length` 判定为截断失败；
  - 空分析结果不再判定为成功；
  - 提示词限制最多 3 条 priorities/risks/suggestions，并限制文本长度；
  - 失败时记录原因并使用本地规则兜底。

### 开发日志

- PC 设置页和侧栏新增“开发日志”。
- 最多保留 500 条日志。
- 记录页面导航、数据保存、JavaScript 错误、AI 请求参数、原始响应、解析、二次整理、兜底、采纳/忽略决定。
- 支持查看详情、复制诊断信息、导出 JSON/文字、清空日志。

## 三、最近验证结论

用户提供的日志：
`C:\Users\y1933\Downloads\workbench-dev-logs-1787298202084.json`

日志包含两次运行：

1. 07:20：修复前旧记录。HTTP 200，但 max_tokens=1200，第一次输出因 `finish_reason=length` 截断；第二次整理因 reasoning 占满预算、content 为空；旧逻辑误记为“二次整理成功”，得到空分析。
2. 07:40：修复后新记录。HTTP 200，`maxTokens=3600`，`finish_reason=stop`，返回合法 JSON；解析成功，生成 3 条优先级、3 条风险、3 条建议，未使用本地兜底。

因此 AI 接入链路和本轮输出修复已验证有效。

仍需观察但未确认故障：AI 请求期间用户切换了页面，请求完成后结果仍写回并保存。未来如果允许重复点击“分析今日”，可能需要增加“请求进行中禁止重复提交/请求编号校验”，防止异步结果互相覆盖。

## 四、核心文件

- `index.html`：桌面端入口
- `scripts/app.js`：桌面端核心逻辑；当前 AI、开发日志主要代码在这里
- `styles/style.css`：桌面端样式
- `local-ai-bridge.js`：PC 本机 AI 中转层
- `www/`：发布/移动端 Web 资源；`www/scripts/app.js`、`www/styles/style.css`、`www/local-ai-bridge.js` 已同步
- `android/app/src/main/assets/public/`：Android 静态资源；脚本已同步
- `mobile.html`、`styles/mobile.css`：移动端页面和样式
- `.workbuddy/memory/MEMORY.md`：项目长期约定
- `.workbuddy/memory/2026-08-21.md`：本日工作记录

## 五、备份目录

按时间顺序：

- `backups/pc-ai-judgement-before-20260821-124912`
- `backups/custom-ai-config-before-20260821-131820`
- `backups/local-ai-bridge-before-20260821-133854`
- `backups/minimal-ai-config-before-20260821-135406`
- `backups/ai-structured-repair-before-20260821-143630`
- `backups/dev-logs-before-20260821-150127`
- `backups/ai-output-fix-before-20260821-152934`

最近一次 AI 输出修复前备份：
`backups/ai-output-fix-before-20260821-152934`

## 六、当前 Git 与工作区状态

- HEAD 在 `e597bb5`，最近一次提交是“add project handoff documentation”。
- 当前工作区有大量未提交修改，包含此前 UI、移动端、AI、文档和配置等历史改动。
- 本轮没有提交、推送或部署。
- 目前 `git status` 显示核心修改包含：`scripts/app.js`、`www/scripts/app.js`、`styles/style.css`、`www/styles/style.css`，以及多个入口、移动端、Android/项目文档文件。
- `local-ai-bridge.js` 和 `www/local-ai-bridge.js` 当前为未跟踪文件；Android 中转层是否需要随 APK 使用，需后续明确，不应误以为它能直接运行在 Android 内。
- 工作区还有 `app-debug.apk`、`app-release.apk`、`backups/`、`deliverables/` 等未跟踪产物或目录。不要清理或删除，除非用户明确要求并逐项确认。

## 七、当前尚未完成/待确认

### 优先级最高

1. 明确是否继续推进 PC AI：下一步建议先测试真实失败场景，而不是立刻改代码。
2. 如果要增强稳定性，先讨论“防重复点击 + 请求编号校验 + 页面切换时结果处理”方案，确认后再备份修改。
3. 继续使用开发日志反馈新问题；出现异常时导出 `workbench-dev-logs-*.json` 并提供给助手。

### 仍未做或未最终确认

- AI 今日判断尚未形成更广泛的真实任务数据测试矩阵。
- 尚未处理请求期间切页/重复请求的竞态保护。
- 尚未建立独立的最新版本号或发布说明。
- 尚未提交当前一系列未提交修改。
- 尚未重新构建 Android APK 以包含最近 AI/日志资源，且本机中转层是 PC Node 服务，不应直接假设 Android 可用。
- 生产部署和商业化暂缓，不要主动扩展。

## 八、本轮新增实现

- 计划中心三种模式的“生成计划草案”已改为优先调用真实 AI：使用统一结构化 JSON 生成器，输出任务、里程碑、交付物、假设、问题和置信度。
- AI 生成失败或未配置时自动使用本地规则草案，页面明确标记来源；不会因为接口失败导致计划中心不可用。
- AI 计划生成结果与已有 Goal → Milestone → Deliverable → Task → ScheduleBlock 数据链路兼容，可继续预览、编辑、备份、权限确认和写入。
- 新增 AI 动作记录：计划生成、计划写入、今日分析、执行状态变化和局部重排写入 `data.__planner.aiActions`，最多保留 200 条。
- 修正 `confidence: 0` 被错误默认成 0.5 的问题，真实 0 会被保留。
- 本轮备份：`backups/ai-workbench-complete-before-20260821-1700`。
- 已同步 `scripts/app.js`、`www/scripts/app.js`、Android 静态资源脚本，以及 PC 样式到 www/Android；语法与同步一致性检查通过。

## 九、新对话的推荐启动方式

新对话开始时直接说明：

> 继续《个人工作台》项目。先读取 `项目衔接快照-2026-08-21.md`、`.workbuddy/memory/MEMORY.md` 和当天工作日志，不要重复回顾历史。当前优先关注 PC AI 和开发日志；修改前先用非技术语言分析并等待确认；不要提交 Git、推送或部署。

如果用户给出新的开发日志，优先读取日志并按“接口 → 请求参数 → 原始响应 → finish_reason → 正式 content → 解析结果 → 页面保存结果”的顺序诊断。
