# 个人工作台 项目总结

> **版本**：v1.6.0  
> **周期**：2026-08-05 ~ 2026-08-06  
> **状态**：已上线，双端可用  
> **访问地址**：  
> - 桌面版：<https://yyzfanliuo.github.io/P_S_v3/>  
> - 移动版：<https://yyzfanliuo.github.io/P_S_v3/mobile.html>

---

## 一、项目概述

「个人工作台」是一个纯前端个人管理工具，覆盖日程、待办、打卡、记账、阅读、运动、笔记等日常场景。无后端、无构建步骤，数据存储在浏览器 `localStorage`，通过 GitHub Gist API 实现跨设备同步。

**技术栈**：原生 HTML + CSS + JavaScript（零依赖、零构建）  
**代码仓库**：<https://github.com/YYZfanliuo/P_S_v3>  
**部署方式**：GitHub Pages（Actions 自动部署）

---

## 二、工作台详情

### 2.1 核心架构

```
index.html          桌面版入口
mobile.html         移动版入口
scripts/app.js      核心逻辑（CONFIG + 渲染 + 同步 + 编辑器），双端共用
styles/style.css    桌面版样式
styles/mobile.css   移动版样式
assets/             头像、横幅、图标等静态资源
```

**配置驱动设计**：全部模块通过 `CONFIG` 对象定义，包括模块名称、图标、颜色、字段类型、种子数据。新增模块只需在 `CONFIG.modules` 中添加一项，编辑器和导航自动渲染。

### 2.2 模块清单（12 个）

| # | 模块 | key | 类型 | 说明 |
|---|------|-----|------|------|
| 1 | 今日计划 | `todo` | todo | 任务清单 + 优先级（P0/P1/P2）+ 勾选完成 |
| 2 | 习惯打卡 | `checkin` | checkin | 每日打卡 + 连续天数统计 |
| 3 | 阅读打卡 | `read` | progress | 书籍进度条（当前页/目标页）+ 摘录笔记 |
| 4 | 每日锻炼 | `sport` | progress | 运动进度（分钟）+ 分类记录 |
| 5 | 记账本 | `money` | finance | 收入/支出 + 分类统计 + 支出四标签 |
| 6 | 心情日记 | `note` | note | 标题 + 正文 + 心情标签（开心/平静/低落/焦虑/疲惫） |
| 7 | 今日热点 | `hot` | note | 热点收藏 + 稍后读标记 |
| 8 | 学习进度 | `learning` | progress | 课程/技能进度追踪（课时） |
| 9 | 食谱与餐单 | `recipes` | note | 食谱收藏 + 菜系/食材/烹饪时间自定义字段 |
| 10 | 项目管理 | `projects` | todo | 个人项目 + 状态/截止日期/优先级 |
| 11 | 我的课程表 | `timetable` | timetable | 大学课表 + 星期/周次/单双周 |
| 12 | 日程管理 | `schedule` | schedule | 每日日程 + 时间轴可视化 + 关联模块 |

### 2.3 模块类型系统

| 类型 | 功能 | 自定义字段支持 |
|------|------|---------------|
| `todo` | 勾选完成 + 优先级 | text / textarea / number / select / date |
| `checkin` | 每日打卡 + 连续天数 | 同上 |
| `progress` | 进度条（当前/目标） | 同上 |
| `finance` | 收支记录 + 分类统计 | 同上 + `spendTag` 支出标签 |
| `note` | 标题 + 正文 + 心情标签 | 同上 |
| `timetable` | 课表 + 星期/周次/单双周 | 同上 |
| `schedule` | 日程 + 时间轴可视化 | 同上 + 颜色选择 + 关联模块 |

### 2.4 首页概览

首页包含以下区域：
- **今日概览环形图**：4 个指标（今日计划完成度、打卡完成度、阅读进度、运动进度）
- **每日一句**：按星期轮换的 7 句励志语
- **本周状态趋势**：用户填写的 7 天状态评分折线图
- **快速记录按钮**：4 个快捷入口（记运动/记打卡/记一笔/记想法）
- **模块卡片**：各模块的当日摘要卡片

### 2.5 设置页功能

- **深色模式**：`data-mode` 属性切换 + CSS 变量覆盖 + localStorage 持久化 + FOUC 防闪
- **数据管理**：导出（JSON 下载）/ 导入（JSON 上传）/ 重置（清除 localStorage）
- **云端同步**：GitHub Gist API 配置（Token + Gist ID）
- **页面背景**：自定义背景图 + 模糊度滑块 + 全卡片透明度控制
- **头像更换**：点击上传，自动压缩 ≤256×256 JPEG 85%

### 2.6 日程管理视图

- 纵轴为小时（0-24），横轴为分钟列
- 事件定位：`grid-row`（小时）+ `left/width` 百分比（分钟）
- 跨小时事件拆分为每小时段，保持同色 + 关联竖线
- 颜色使用饱和深色（`#a87020`、`#5588bb` 等），自动白色文字（`contrastText()` 函数）
- 时间输入采用**滚轮选择器**（非滑块），符合用户偏好

### 2.7 记账支出四标签

每笔支出可选择标签，统计卡片按标签汇总：

| 标签 | 颜色 | 说明 |
|------|------|------|
| 必要固定支出 | 蓝色 | 房租、保险等固定必要支出 |
| 不必要固定支出 | 黄色 | 订阅服务等固定但非必要 |
| 必要不固定支出 | 绿色 | 餐饮、交通等变动必要支出 |
| 不固定不必要支出 | 红色 | 冲动消费等 |

---

## 三、跨设备数据同步

### 3.1 同步架构

```
设备A (localStorage)  ←→  GitHub Gist API  ←→  设备B (localStorage)
```

- **协议**：GitHub Gist REST API
- **认证**：Classic Token（需 `gist` 权限）
- **数据格式**：`workbench-data.json`（全部用户数据的 JSON 序列化）
- **冲突处理**：基于服务器时间戳 `updated_at`，新数据覆盖旧数据

### 3.2 ETag 信号检测（v1.6.0 核心功能）

**问题**：原方案每 30 秒全量拉取 Gist 内容，带宽浪费严重，且延迟较高。

**方案**：利用 HTTP `If-None-Match` / `ETag` / `304` 机制：

```
每 5 秒 → 发送 If-None-Match: <上次ETag>
  ├─ 304 (未变) → 跳过，不拉取数据
  └─ 200 (有变) → 记录新 ETag → 立即拉取数据
```

**效果**：
- 带宽降低 99%（304 响应仅头部，无 body）
- 延迟从 30 秒降至 5 秒
- 一端推送后，Gist 内容哈希变化（ETag 变），对端 5 秒内感知并同步

### 3.3 防循环机制

- **推送后抑制**：`push` 成功后重置 `syncSignalEtag`，确保下次探测能感知到自己的变更
- **拉取后抑制**：`pull` 后 5 秒内不触发 `push`，避免拉取引起的数据变化被误判为本地编辑

### 3.4 时间戳校正

- 同步判断使用 Gist 的 `updated_at`（GitHub 服务器时间），不依赖客户端时钟
- 解决了设备间时钟不一致导致同步失效的问题

---

## 四、双端适配

### 4.1 桌面版

- 左侧固定侧栏（导航 + 头像 + 版本号）
- 右侧主内容区
- 宽屏布局，卡片网格排列
- CSS 变量控制透明度（`--sidebar-opacity`、`--card-opacity`）

### 4.2 移动版

- 单列布局
- 底部导航栏（Tab Bar）
- 左滑抽屉菜单
- 自动识别移动设备，适配单手操作
- 与桌面版共享同一份 `CONFIG` 和 `app.js`

### 4.3 共享约束

- 桌面版和移动版**必须共享同一份 CONFIG**（`full` 模式）
- 数据结构完全一致，通过 Gist 同步无缝衔接

---

## 五、部署方案

### 5.1 GitHub Pages + Actions

```yaml
# .github/workflows/deploy-pages.yml
触发: push to main
步骤:
  1. 检出代码
  2. 上传 artifact（整个仓库根目录）
  3. 部署到 GitHub Pages
```

- **Token 要求**：需 `repo` + `workflow` + `gist` 权限
- **自动触发**：每次 `git push main` 自动部署
- **部署时间**：通常 1-2 分钟

### 5.2 部署排障经验

| 问题 | 根因 | 解决方案 |
|------|------|---------|
| Jekyll 构建失败 | `SKILL.md` 的 YAML front matter 触发 Jekyll 解析 | 使用 Actions 部署（跳过 Jekyll）或添加 `.nojekyll` |
| Token 403 拒绝 | Token 缺少 `workflow` 权限 | 创建带 `workflow` 权限的 Classic Token |
| YAML 语法损坏 | PowerShell 双引号 here-string 破坏 `${{ }}` | 使用单引号 here-string `@'...'@` |
| Pages 卡在 building | 反复切换部署模式导致状态损坏 | 删除 Pages 站点后重新创建 |

---

## 六、开发里程碑

### P1 核心功能（2026-08-05）

- [x] 深色模式切换（`data-mode` + CSS 变量 + FOUC 防闪）
- [x] 数据导出/导入/重置
- [x] 模块示例种子数据
- [x] 洞察页统计（今日计数 + 进度条）

### P2 体验增强（2026-08-05）

- [x] 首页进度卡片步进按钮（−/+）
- [x] 搜索覆盖自定义字段（如食谱食材）
- [x] 记账月度切换筛选

### 功能扩展（2026-08-05）

- [x] 支出四标签分类与统计
- [x] 全页背景图 + 模糊度控制
- [x] 全卡片透明度统一控制
- [x] 版本号显示（侧栏底部 + 缓存破坏参数）

### 部署与同步（2026-08-05 ~ 06）

- [x] GitHub Pages 部署（Actions 模式）
- [x] 移动版双端上线
- [x] GitHub Gist 数据同步
- [x] ETag 信号检测实时同步（v1.6.0）
- [x] 防循环推送机制
- [x] 服务器时间戳校正

---

## 七、工程约定

1. **配置驱动**：所有模块通过 `CONFIG` 对象定义，新增模块无需改框架代码
2. **CSS 变量系统**：颜色、透明度、背景图等均通过 CSS 变量控制，支持深色模式覆盖
3. **日期处理**：使用 `localDateStr(d)` 而非 `toISOString().slice(0,10)`，避免时区偏移
4. **事件颜色**：使用饱和深色 + `contrastText()` 自动白字，保证对比度
5. **时间输入**：滚轮选择器（非滑块），符合用户操作习惯
6. **版本号**：`app.js?v=13` 查询参数破坏缓存
7. **深色模式**：`data-mode` 属性（非 `data-theme`），避免浏览器自动化工具冲突
8. **同步时间戳**：使用 Gist `updated_at` 服务器时间，不依赖客户端时钟

---

## 八、核心设计原则

### 8.1 零运维

- 无后端、无数据库、无构建步骤
- 数据在 `localStorage`，同步走 Gist API（免费）
- 部署靠 GitHub Actions 自动触发

### 8.2 配置驱动

- 新增模块 = `CONFIG.modules` 加一项
- 自定义字段自动渲染编辑器（text/textarea/number/select/date）
- 搜索自动覆盖所有自定义字段

### 8.3 CSS 变量视觉系统

```css
:root {
  --bg, --bg2, --bg3        /* 背景层次 */
  --ink, --muted            /* 文字层次 */
  --accent, --module-N      /* 主题色 + 模块色 */
  --sidebar-opacity         /* 侧栏透明度 */
  --card-opacity            /* 全卡片透明度 */
  --page-bg-image           /* 背景图 */
  --page-bg-blur            /* 背景模糊度 */
}
:root[data-mode="dark"] { /* 深色模式覆盖上述变量 */ }
```

### 8.4 ETag 信号探测

- 轮询频率：5 秒
- 304 = 无变更，跳过
- 200 = 有变更，立即拉取
- 带宽降低 99%，延迟降至 5 秒

---

## 九、项目交付物

| 交付物 | 类型 | 地址 |
|--------|------|------|
| 桌面版 | 在线应用 | <https://yyzfanliuo.github.io/P_S_v3/> |
| 移动版 | 在线应用 | <https://yyzfanliuo.github.io/P_S_v3/mobile.html> |
| GitHub 仓库 | 源代码 | <https://github.com/YYZfanliuo/P_S_v3> |
| 部署排障经验 | HTML 文档 | `github-pages-deploy-experience/github-pages-deploy-experience.html` |
| 项目经验总结 | HTML 文档 | `project-experience-summary/project-experience-summary.html` |
| 可复用知识库 | HTML 文档 | `GitHub-Pages部署与跨端数据同步实战经验.html` |

---

## 十、后续优化方向

1. **日程周/月视图**：当前仅日视图，可扩展周视图和月视图
2. **数据统计图表**：记账月度趋势、阅读进度曲线、运动热力图
3. **PWA 离线支持**：Service Worker 缓存，离线可访问
4. **多设备同步状态**：显示最后同步时间、设备列表
5. **模块自定义排序**：拖拽调整模块顺序
6. **数据加密**：Gist 数据加密存储，保护隐私
7. **导入导出增强**：支持 CSV 导出、Excel 导入
8. **通知提醒**：日程提醒、打卡提醒（Notification API）

---

*文档生成时间：2026-08-06*  
*项目版本：v1.6.0*  
*维护者：用户 + Trae AI 助手*
